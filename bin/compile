#!/bin/sh

#Verify MITIE LIB
if [ ! -d $1/MITIE ]; then
	echo "---------> Missing MITIE LIB"
	exit 1
fi

#Compile LIB
cd $1/MITIE/mitielib
make

cd mitielib
mkdir build
cd build
cmake ..
cmake --build . --config Release --target install
